# Sevoniva Docs

Sevoniva 文档站点，基于 [Fumadocs](https://fumadocs.dev) + Next.js 构建。

## 本地开发

```bash
pnpm install
pnpm dev
```

访问 http://localhost:3000

## 写文档

所有文档位于 `content/docs/` 目录，使用 `.mdx` 格式。

提交到 `main` 分支后，GitHub Actions 会自动构建并部署到 https://sevoniva.github.io
